#include "vehicle.h"

using namespace std;

std::pair<float, float> Vehicle::calculateVehiclePositionInTime(float currentTime) {
	return pair(0, 0);
}

float Vehicle::distanceToPosition(const std::pair<float, float> position) {
	return 1.0;
}